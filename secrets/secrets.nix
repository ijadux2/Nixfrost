{ ... }:

{
  # Secrets are managed via agenix
  # See secrets/AGE_KEY.txt for your public key
  # To add secrets:
  # 1. Create encrypted file: secrets/<name>.age
  # 2. Add to age.secrets in this file
}
